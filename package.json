{
  "name": "www.mahata.org",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --package=serve@14.2.6 dlx serve public",
    "lint": "biome check .",
    "format": "biome format --write .",
    "deploy": "pnpm --package=netlify-cli@27.8.1 dlx netlify deploy --prod --dir=public --site \"$NETLIFY_SITE_ID\" --no-build"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.12"
  },
  "packageManager": "pnpm@10.4.1"
}
